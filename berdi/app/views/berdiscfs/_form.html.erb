<%= form_for @berdiscf, :html => { :class => "form-horizontal berdiscf" } do |f| %>

    <% if @berdiscf.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@berdiscf.errors.count, "error") %> prohibited this berdiscf from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @berdiscf.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>



  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            berdiscfs_path, :class => 'btn btn-default' %>

<% end %>
